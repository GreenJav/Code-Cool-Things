package blabla;

public class q6 {
    /* a) A sintaxe do if está errada. Em if (age>=65); deveria ser if (age>=65){ , há o posicionamento incorreto do ";".

    b) A variavel total não é iniciada, portanto ao executar o codigo haveria erro no programa visto que não se sabe com
    o que está sendo somado x na primeira iteração do laço.

    c) Há um grupo de sentenças no laço while, nesse caso, deveria estar entre chaves "{}" para que a sintaxe estivesse
    correta, o que não acontece nesse trecho.

    d) Nesse laço há varias sentenças e inicia-se o agrupamento delas com as chaves, porém esse grupo não é fechado. Ou
    seja, o grupo de senteças não está fechado entre chaves "{}"
     */
}
